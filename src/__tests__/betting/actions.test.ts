/**
 * Tests for betting actions
 * Note: Betting actions are tested in player/actions integration tests
 */

import { describe, it, expect } from '@jest/globals';

describe('Betting Actions', () => {
  it('should be tested in player actions integration tests', () => {
    expect(true).toBe(true);
  });
});
